uds:
  control_socket: "DirectScreenAPI/artifacts/run/dsapi.sock"
  data_socket: "DirectScreenAPI/artifacts/run/dsapi.data.sock"

timeouts:
  socket_timeout_ms: 30000
  frame_wait_timeout_ms: 250
  reconnect_delay_ms: 120

image:
  max_long_edge: 1600
  jpeg_quality: 90
  jpeg_subsampling: 0
  enable_sharpen: true
  sharpen_radius: 1.2
  sharpen_percent: 135
  sharpen_threshold: 3
  enable_axis: true
  axis_step: 100
  axis_tick_size: 26
  axis_line_width: 4
  axis_font_size: 0

gesture:
  default_swipe_duration_ms: 300
  default_long_press_duration_ms: 1000
  default_drag_hold_ms: 500
  default_drag_duration_ms: 500
  zoom_default_distance: 400
  zoom_default_duration_ms: 300

system_control:
  enable: true
  use_su: true
  su_bin: "su"
  input_bin: "input"
  command_timeout_ms: 3000
  keycodes:
    back: "KEYCODE_BACK"
    home: "KEYCODE_HOME"
    recents: "KEYCODE_APP_SWITCH"
    volume_up: "KEYCODE_VOLUME_UP"
    volume_down: "KEYCODE_VOLUME_DOWN"
